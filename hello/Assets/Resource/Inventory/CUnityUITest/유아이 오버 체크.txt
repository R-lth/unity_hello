Using UnityEngine.EventSystems; //이걸 꼭 해야함.
 
void Update()
{
        //UI위에 커서가 있을때 = ture/ 따라서 UI위에 커서가 없을때만 ClickAction함수를 실행
        if (EventSystem.current.IsPointerOverGameObject() == false)
        {
            ClickAction();
        } 
        Showmethemoney();
}